# Bakunawa v3 Enhancement Plan

**Goal:** Make Bakunawa more powerful than System-Cleaner-main by fixing bugs, completing JSON-driven architecture, implementing parallel execution, adding new cleaning targets, and modernizing tests.

---

## Phase 1: Bug Fixes & Code Cleanup

### 1.1 Remove duplicate functions in Core.psm1
- **File:** `src/Bakunawa.Core.psm1`
- Delete duplicate `Test-IsDisposableLogPath` (lines 278-292)
- Delete duplicate `Get-DisposableLogCandidates` (lines 294-316)

### 1.2 Remove duplicate function in UI.psm1
- **File:** `src/Bakunawa.UI.psm1`
- Delete duplicate `Show-HealthDetail` (lines 221-238)

### 1.3 Fix SECURITY.md branding
- **File:** `SECURITY.md`
- Line 5: `System Cleaner` → `Bakunawa`
- Line 84: `SystemCleaner.ps1` → `Bakunawa.ps1`

### 1.4 Fix stale RunningProcesses in multi-pass sessions
- **File:** `src/Bakunawa.Cleanup.psm1`
- Add `$script:RunningProcesses = Get-RunningProcessNames` at start of `Invoke-CleanupRun`

### 1.5 Add missing script state declarations to Core.psm1
- **File:** `src/Bakunawa.Core.psm1`
- Add `$script:UiStopwatch`, `$script:LastUiMs`, `$script:UiTickMs` to script state block

---

## Phase 2: JSON-Driven App Cleanup (Complete Architecture)

### 2.1 Refactor Clear-AppCaches to consume JSON definitions
- **File:** `src/Bakunawa.Cleanup.psm1`
- Replace hardcoded switch/case with iteration over `Get-AllAppDefinitions`
- For each app in JSON: resolve paths via `Resolve-EnvTemplate`, apply process-aware skipping, call `Measure-AndClear`
- Handle special templates: `{profile}` (browsers), `{sub:cache}` (Telegram/Stremio), `{product}` (JetBrains)
- Keep Playwright and Go as warn-only exceptions (not in JSON locations)

### 2.2 Refactor Clear-DevCaches similarly
- **File:** `src/Bakunawa.Cleanup.psm1`
- Same pattern: iterate devtools.json instead of hardcoded blocks
- Preserve warn-only behavior for Playwright and Go

### 2.3 Refactor Clear-ChromiumCaches to use browsers.json
- **File:** `src/Bakunawa.Cleanup.psm1`
- Replace hardcoded browser list with iteration from `Get-AppDefinitions('browsers')`
- Expand `{profile}` templates per-browser

### 2.4 Add new app definition JSON files
- **New file:** `app-definitions/games.json` — Steam, Epic Games, Battle.net, GOG Galaxy, EA App, Minecraft, Unity
- **New file:** `app-definitions/cloud.json` — OneDrive, Google Drive, Dropbox
- **New file:** `app-definitions/creative.json` — Adobe (Photoshop/Premiere/After Effects), DaVinci Resolve, VLC
- **New file:** `app-definitions/productivity.json` — Microsoft Office, 1Password, Bitwarden, Postman
- **New file:** `app-definitions/devops.json` — Terraform, Gradle, Maven, GitHub CLI

### 2.5 Wire new JSON categories into cleanup orchestration
- **File:** `src/Bakunawa.Cleanup.psm1`
- Add new cleanup tasks: "Game Caches", "Cloud Sync Caches", "Creative App Caches", "Productivity Caches", "DevOps Caches"
- Register in `Get-CleanupTasks` for Standard mode

---

## Phase 3: Parallel Execution

### 3.1 Implement runspace pool for independent cleanup tasks
- **File:** `src/Bakunawa.Cleanup.psm1`
- Create `Invoke-ParallelCleanup` function using `RunspacePool`
- Tasks marked `Parallel = $true` (Browser Caches, App Caches, Dev Caches, Game Caches, Cloud Sync, Creative, Productivity, DevOps) run concurrently
- Sequential tasks (System Caches, GPU/Shell, Recycle Bin, Log Files, Empty/Stale, Orphan Scan) run before/after parallel block
- Collect results and errors from runspace pool
- Max concurrency configurable (default: 4 runspaces)

### 3.2 Update Invoke-CleanupRun to use parallel dispatch
- Replace sequential foreach with: sequential pre-tasks → parallel block → sequential post-tasks
- Preserve UI step tracking (start/finish steps around parallel block)

---

## Phase 4: New Cleaning Targets

### 4.1 Game cache cleanup
- Steam shader cache (`steamapps/shadercache`), logs, `appcache/packageinfo_*`
- Epic Games Launcher saved data caches
- Battle.net/Blizzard cache
- GOG Galaxy cache
- EA App cache
- Minecraft packages cache
- Unity Editor cache

### 4.2 Cloud sync cache cleanup
- OneDrive logs and transfer cache
- Google Drive File Stream logs/cache
- Dropbox cache and logs

### 4.3 Creative app cache cleanup
- Adobe temp files and media cache
- DaVinci Resolve cache and logs
- VLC cache

### 4.4 Productivity app cache cleanup
- Microsoft Office logs and cache
- 1Password/Bitwarden logs
- Postman cache

### 4.5 DevOps tool cache cleanup
- Terraform plugin cache
- Gradle caches
- Maven repository cache
- GitHub CLI cache

---

## Phase 5: Test Modernization

### 5.1 Update all test files to Pester v5 syntax
- **Files:** All 5 test files in `tests/`
- `Should Be` → `Should -Be`
- `Should BeNullOrEmpty` → `Should -BeNullOrEmpty`
- `Should Not BeNullOrEmpty` → `Should -Not -BeNullOrEmpty`
- `Should Match` → `Should -Match`
- `Should BeGreaterThan` → `Should -BeGreaterThan`
- `Should BeLessThan` → `Should -BeLessThan`
- `Should BeOfType` → `Should -BeOfType`
- `Should Not Throw` → `Should -Not -Throw`
- Add `BeforeAll`/`AfterAll` blocks where needed for Pester v5 discovery

### 5.2 Add new test cases
- Test JSON-driven app definition loading for new categories
- Test parallel execution (mocked runspace pool)
- Test new app targets (games, cloud, creative, etc.)

---

## Verification

1. **Syntax check:** Run `pwsh -Command "Import-Module ./src/Bakunawa.Core.psm1; Import-Module ./src/Bakunawa.Config.psm1; Import-Module ./src/Bakunawa.Cleanup.psm1; Import-Module ./src/Bakunawa.UI.psm1"` — no errors
2. **Module import:** Run `pwsh -Command "Import-Module ./Bakunawa.ps1 -Force"` — no duplicate function warnings
3. **Preview mode:** Run `pwsh -File ./Bakunawa.ps1 -Mode Preview` — should enumerate all new targets without errors
4. **Tests:** Run `pwsh -Command "Invoke-Pester ./tests/ -Output Detailed"` — all tests pass with v5 syntax
5. **Parallel execution:** Verify browser/app/dev caches run concurrently in Standard mode
6. **JSON definitions:** Verify new categories (games, cloud, creative, productivity, devops) load and cleanup correctly
